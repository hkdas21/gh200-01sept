name: timeout

on: push

jobs:
  setup:
    runs-on: ubuntu-latest
    timeout-minutes: 10              # Job timeout
    steps:
      - name: ⏱️ Step with Timeout
        timeout-minutes: 1           # Step timeout
        run: |
          sleep 90
          echo "This step must complete in 1 minute"
